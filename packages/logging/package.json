{
  "name": "@geersch/logging",
  "version": "1.0.0",
  "description": "A simple logging package used for demo purposes.",
  "main": "dist/index",
  "types": "dist/index",
  "author": "Christophe Geers",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:geersch/packages.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\"",
    "lint": "eslint '**/**.ts' --fix",
    "prepublishOnly": "yarn run build",
    "test": "jest",
    "test:cov": "jest --coverage"
  },
  "peerDependencies": {
    "chalk": "^4.1.0"
  },
  "devDependencies": {
    "chalk": "^4.1.0"
  }
}
