{
  "name": "@geersch/calculator",
  "version": "1.0.3",
  "description": "A simple calculator class used for demo purposes.",
  "main": "dist/index",
  "types": "dist/index",
  "author": "Christophe Geers",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:geersch/packages.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "lint": "echo \"Error: run tests from root\" && exit 1",
    "prepublishOnly": "yarn run build",
    "test": "echo \"Error: lint code from root\" && exit 1"
  },
  "devDependencies": {
    "@geersch/logging": "^0.9.1"
  },
  "peerDependencies": {
    "@geersch/logging": "^0.9.0 || ^1.0.0"
  }
}
